// gematria-core — shared cipher engine + number theory + checksum.
// Canonical copies extracted from apps/mlb/src/engine (LAYOUT-SPEC §2 dedup item).
// apps/wnba imports from this package; apps/mlb still carries its local copy
// (identical content, verified at extraction) and migrates on its next touch.
export * from "./gematria.js";
export * from "./numbers.js";
